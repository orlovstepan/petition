<h1>Who else signed the petition</h1>
<ul>
    {{#rows}}
        <li>
            {{first}}
            {{last}}
            |
            {{age}}
            |
            <a href="/signers/{{city}}">{{city}}</a>
        </li>
    {{/rows}}

</ul>